import 'package:flutter/material.dart';
import 'Search_Screen.dart';

class SearchPassengerScreen extends StatelessWidget {
  const SearchPassengerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SearchScreen(screenNumber: 2));
  }
}
