import 'package:flutter/material.dart';
import '../../../rides/presentation/screens/search_screen.dart';

class SearchPassengerScreen extends StatelessWidget {
  const SearchPassengerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SearchScreen(screenNumber: 2);
  }
}
